const age = 19;
const oldEnough = age >= 18;
if (age >= 18) {
  console.log (`Con ${age} años eres mayor de edad`);
  
} else { const yearLeft = (18 - age);
  console.log(`Con ${age} años eres menor de edad, espera otros ${yearLeft} años`)
  }


/*
//DETERMINAR SI UN NUMERO ES PAR O DISPAR
// EJ1. IF AND ELSE

// const num = Number(prompt ("Numero"));
// if (num %2==0){alert (`El ${num} es par`);}
// else {alert (`El ${num} es impar`);}

//DIVISIBLE POR 5

// const num = Number(prompt ("Numero"));
// if (num %5==0){alert (`El ${num} SI es divisible`);}
// else {alert (`El ${num} NO es divisible`);}

//EJ2. OPERADOR TERCIARIO

// const num = Number(prompt ("Numero"));
// alert (num %2==0 ? (`el ${num} es par`) : (`el ${num} es impar`));
*/

//DETERMINAR SI UN NUMERO ES DIVISIBLE POR 5 Y 11

// const num = Number (prompt ("Numero?"));
// alert (num % 5==0 && num %11==0 ?  (`si`) : (`no`));

