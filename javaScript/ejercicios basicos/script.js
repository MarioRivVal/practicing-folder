"use strict"
/*
//propia prueba,tambien sin parentesis {}
//  const age=15;
//  if (age>=18)console.log("Mayor de edad");
//  else console.log("Menor de edad");
*/

/*
// propia prueba
// const respuesta =prompt("Ana quiere a Mario?");

// if (respuesta==="si"){console.log("Mario esta feliz con tu respuesta");}
// else if (respuesta==="no"){console.log("Ahora Mario esta triste con tu respuesta");}
// else {console.log("Esa no es una respuesta");}
*/
/*
// Propio ejercicio:

//escenario de bonus por ventas
const salary = 1000;
const octSales = 70;

const bonusTereza = octSales >=500 ? salary + 60 : salary + 10;

console.log (bonusTereza);

//escenario bonus por venta en pantalla

const salary = 1000;
const monthSales = Number(prompt ("Venta total del mes"));

const bonusSaler = monthSales >=500 ? salary + 60 : salary;

alert (`Total: ${bonusSaler}`);

//suma de numero pedidos en pantalla

const priNum = Number (prompt ("First Number"));
const segNum = Number (prompt ("Second Number"));

const total = priNum + segNum;
console.log (total);

// operador terciario
// escenario de calculo tips (propinas)

const bill = 505
const tip = bill >= 50 && bill <= 500 ? bill * 0.15 : bill * 0.20;
console.log (` The bill is ${bill}, tip is ${tip} and total is ${bill + tip}`);

// escenario de calculo tips (propinas) en pantalla

const bill = Number (prompt ("Bill"));
const tip = bill > 1 && bill < 200 ? bill * 0.15 : bill * 0.20;
alert (`final tip, ${bill + tip}`);
*/

/*
//DETERMINAR SI UN NUMERO ES PAR O DISPAR
// EJ1. IF AND ELSE

const num = Number(prompt ("Numero"));
if (num %2==0){alert (`El ${num} es par`);}
else {alert (`El ${num} es impar`);}

//EJ2. OPERADOR TERCIARIO

const num = Number(prompt ("Numero"));
alert (num %2==0 ? (`el ${num} es par`) : (`el ${num} es impar`));
*/